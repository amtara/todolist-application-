import React from 'react'

const EditionModalComponent =(props) => {
    return (
        <div className={`${props.isopen ? "block" : "hidden"} absolute bg-gray-500 p-5 inset-0`}>
whd
        </div>
    )
}

export default EditionModalComponent
